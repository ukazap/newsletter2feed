<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Newsletter2Feed" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Newsletter2Feed">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <a href="/">
      <img src="/icon.svg" width="60">
    </a>

    <% if notice.present? %>
      <div class="notice"><%= notice %></div>
    <% end %>
    <% if alert.present? %>
      <div class="alert"><%= alert %></div>
    <% end %>

    <%= yield %>

    <footer>
      <p>Made by <a href="https://github.com/ukazap">@ukazap</a>. Inspired by <a href="https://github.com/leafac/kill-the-newsletter"><em>Kill the Newsletter!</em></a></p>
    </footer>
  </body>
</html>
